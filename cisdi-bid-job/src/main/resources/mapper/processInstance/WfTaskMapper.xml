<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pms.bid.job.mapper.processInstance.WfTaskMapper">
    <resultMap id="WfTaskResultMap" type="com.pms.bid.job.domain.processInstance.WfTask"></resultMap>

    <!--新增任务-->
    <insert id="create">
        insert into wf_task (
            ID,
            WF_NODE_INSTANCE_ID,
            AD_USER_ID,
            RECEIVE_DATETIME,
            VIEW_DATETIME,
            ACT_DATETIME,
            IS_CLOSED,
            LK_WF_INST_ID,
            STATUS,
            CRT_DT,
            CRT_USER_ID,
            LAST_MODI_DT,
            LAST_MODI_USER_ID,
            WF_TASK_TYPE_ID,
            SEQ_NO,
            IN_CURRENT_ROUND,
            TS,
            WF_PROCESS_ID,
            WF_PROCESS_INSTANCE_ID,
            WF_NODE_ID,
            AD_ACT_ID,
            VER
        ) values (
            #{id},
            #{wfNodeInstanceId},
            #{adUserId},
            #{receiveDateTime},
            #{viewDateTime},
            #{actDateTime},
            #{isClosed},
            #{wfProcessInstanceId},
            #{status},
            #{createDate},
            #{createBy},
            #{lastUpdateDate},
            #{lastUpdateBy},
            #{wfTaskTypeId},
            #{seqNo},
            #{isCurrentRound},
            #{ts},
            #{wfProcessId},
            #{wfProcessInstanceId},
            #{wfNodeId},
            #{adActId},
            #{ver}
        )
    </insert>

    <update id="closeTask">
        update  wf_task set is_close = 1 where  id = #{id}
    </update>
</mapper>